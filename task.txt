Не выполняя код, ответить, что выведет браузер и почему:
 
a. 
if (!("a" in window)) {
    var a = 1;
}
alert(a);

window является глобальным объектом с кучей методов и "а" скорее всего в нем присутствует, значит выражение во внутренних  скобках вернет true, но так как в условии перед скобками стоит отрицание "!", то в проверку условия попадет false и переменной а не будет присвоено значение 1, соответственно браузер выведет ошибку что значение не определено.

b.
var b = function a(x) {
    x && a(--x);
};
alert(a);

Здесь функция а(х) присваивается переменной b и в алерте  "а" будет вызывается как переменная, которая нигде у нас не объявлена, т.е. по идее выдаст так же ошибку что значение не определено

с.
function a(x) {
    return x * 2;
}
var a;
alert(a);

В этои случае при объявлении фуцнкции она присваивается имени функции, т.е. переменной а. соответственно браузером будет выведен текст функции а.

d.
function b(x, y, a) {
    arguments[2] = 10;
    alert(a);
}
b(1, 2, 3);

Ну здесь браузер выведет 10, т.к. внутри функции мы присваеваем 2 аргументу, т.е. "а"( т.к. нумерация идет с 0) заначение 10, не зависимо от того какое передано в функцию при ее вызове.


е.
*
function a() {
    alert(this);
}
a.call(null);

Метод call() первый параметр берет в качестве this, соответственно у нас будеn alert(null) и браузер выведет null, но потестив в браузере, я получаю "object Window",  а не null. почему так получается я объяснения не нашел, либо я не так понял как работает call.